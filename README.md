# ultra-results-db
   Normalized Postgres database of ultramarathon results and splits, sourced from timing platforms and race databases via a pluggable ingestion architecture. dbt + Python + entity resolution across sources.
